# YuanForge Lessons

只记录已经发生、可复用且会改变后续做法的经验。

## 复制目录必须区分首次安装与重复安装

- 症状：目标 Skill 目录已存在时，`cp -R source target` 会产生 `target/yuanforge/` 嵌套目录。
- 根因：复制命令把源目录本身放进了既有目标，而不是复制源目录内容。
- 预防：复制 `source/.` 到已创建的目标目录，并用自动化测试锁定安装命令。
- 证据：首次自举审计中的重复安装复现。

## 公开仓库发布前必须检查身份与敏感文本

- 症状：介绍文案可能把不应公开的组织信息写入提交历史。
- 根因：只审查当前页面效果，没有把 Git 历史和公开边界作为发布验收项。
- 预防：发布前检查工作树、提交历史、远端元数据和敏感文本；发现历史泄露时不能只修改最新文件。
- 证据：首次发布过程中的用户纠正与仓库重建。

## 结构校验不能替代文本编码校验

- 症状：UI 元数据通过 Skill 结构校验，但 Python 按 UTF-8 读取时失败。
- 根因：Windows 下的生成工具使用了系统默认编码，结构校验没有覆盖编码契约。
- 预防：生成中文元数据后执行 UTF-8 读取测试，并让仓库测试覆盖全部维护文本。
- 证据：首次自举时 `openai.yaml` 的编码失败及修复后回归测试。

## 当前实现不能替代目标交互意图

- 症状：中途 UI 项目的基线完整记录了后端业务流水线、API 和访问面，却没有角色逐页完成任务的产品旅程。
- 根因：把代码和测试对当前行为的强证据，误当成了用户对目标体验的确认；同时混淆产品旅程、业务处理流和交付流程。
- 预防：建立基线时分开当前事实、目标意图和候选方案；用户交互型软件必须覆盖角色、入口、页面、动作、状态、成功、错误恢复、路由和权限，目标意图缺失时逐问澄清。
- 证据：首个真实中途 UI 项目的用户评审，以及其设计讲义与生成基线的对照。
