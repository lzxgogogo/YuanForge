# YuanForge Lessons

只记录已经发生、可复用且会改变后续做法的经验。

## 复制目录必须区分首次安装与重复安装

- 症状：目标 Skill 目录已存在时，`cp -R source target` 会产生 `target/yuanforge/` 嵌套目录。
- 根因：复制命令把源目录本身放进了既有目标，而不是复制源目录内容。
- 预防：复制 `source/.` 到已创建的目标目录，并用自动化测试锁定安装命令。
- 证据：首次自举审计中的重复安装复现。

## 公开仓库发布前必须检查身份与敏感文本

- 症状：介绍文案可能把不应公开的组织信息写入提交历史。
- 根因：只审查当前页面效果，没有把 Git 历史和公开边界作为发布验收项。
- 预防：发布前检查工作树、提交历史、远端元数据和敏感文本；发现历史泄露时不能只修改最新文件。
- 证据：首次发布过程中的用户纠正与仓库重建。

## 结构校验不能替代文本编码校验

- 症状：UI 元数据通过 Skill 结构校验，但 Python 按 UTF-8 读取时失败。
- 根因：Windows 下的生成工具使用了系统默认编码，结构校验没有覆盖编码契约。
- 预防：生成中文元数据后执行 UTF-8 读取测试，并让仓库测试覆盖全部维护文本。
- 证据：首次自举时 `openai.yaml` 的编码失败及修复后回归测试。

## 当前实现不能替代目标交互意图

- 症状：中途 UI 项目的基线完整记录了后端业务流水线、API 和访问面，却没有角色逐页完成任务的产品旅程。
- 根因：把代码和测试对当前行为的强证据，误当成了用户对目标体验的确认；同时混淆产品旅程、业务处理流和交付流程。
- 预防：建立基线时分开当前事实、目标意图和候选方案；先确认角色、触发情境、用户目标、完成信号和关键失败责任，再把页面、路由和组件作为实现表达。
- 证据：首个真实中途 UI 项目的用户评审，以及其设计讲义与生成基线的对照。

## 领域执行能力不进入基线治理核心

- 症状：把“领域探针”直接实现成多个独立 Skill，会先引入安装、发现、调用和维护成本，却还没有跨仓库证据证明这些能力需要独立生命周期。
- 根因：把基线治理与领域执行混成一个 Skill，导致核心范围和依赖持续膨胀。
- 预防：YuanForge 只识别证据缺口并推荐最小能力组合；领域 Skill 负责自身执行，统一报告负责交接，项目文档保存最终事实。
- 证据：用户在首次探针 Skill 初始化前明确要求优先调用已有好用的 Skill，避免把第一版范围扩大为新的 Skill 生态。

## 用户确认不是要求用户预先知道答案

- 症状：把“目标意图必须确认”执行成要求用户一次说清完整需求，仍然无法处理新项目和模糊想法。
- 根因：忽略意图经常通过情境、候选方案、原型和反馈逐步形成，而不是等待被完整提取。
- 预防：先检查素材，从最高影响未知项开始，一次问一个问题；必要时提供明确标为 `proposed` 的选项或低成本原型，关键取舍确认后再写稳定目标文档。
- 证据：本轮用户逐步区分“还原现状、发现意图、用户确认和新项目文档生成”的连续纠正。

## 真实仓库评测必须区分本地原始产物与可提交记录

- 症状：跨仓库前向评测把真实项目名、提交哈希、本地目录和业务细节直接写入可提交结果。
- 根因：把本地取证所需的可追溯性等同于公共评测记录所需的证据强度，没有执行发布边界检查。
- 预防：原始输出只保留在本地不可提交上下文；仓库内结果使用样本代号、抽象项目类型和非识别性行为结论，并用自动化测试阻止真实名称、哈希和绝对路径进入评测文件。
- 证据：本轮跨仓库评测后的用户发布边界纠正。
